package com.example.hrwebsite.config;

import org.springframework.context.annotation.Configuration;

/**
 * Application configuration for various beans
 */
@Configuration
public class AppConfig {
    // Configuration class for future general application beans
    // PasswordEncoder is now handled in SecurityConfig
}